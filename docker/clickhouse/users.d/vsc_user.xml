<clickhouse>
    <users>
        <vcs_user>
            <password>dev_password</password>
            <networks>
                <ip>::/0</ip> <!-- Allow from anywhere (use VPN/IP filter in prod) -->
            </networks>
            <profile>default</profile>
            <quota>default</quota>
        </vcs_user>
    </users>
</clickhouse>